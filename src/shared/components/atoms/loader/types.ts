/**
 * Loader size options
 */
export type LoaderSize = "sm" | "md" | "lg";

/**
 * Loader component props interface
 */
export interface LoaderProps {
  /**
   * Loader size
   * @default "md"
   */
  size?: LoaderSize;
}

